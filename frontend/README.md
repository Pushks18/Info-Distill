# Frontend Setup

React + Vite frontend for Info-Distill.

## Prerequisites

- Node.js 18+
- npm

## Install

```bash
npm install
```

## Run

```bash
npm run dev
```

The UI expects backend API at `http://localhost:8000`.

For full project setup, see the root `README.md`.
